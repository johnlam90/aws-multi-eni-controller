{
  "spec": {
    "versions": [
      {
        "name": "v1alpha1",
        "schema": {
          "openAPIV3Schema": {
            "properties": {
              "spec": {
                "properties": {
                  "interfaceName": {
                    "description": "Explicit interface name to use for this ENI (e.g., eth2)",
                    "type": "string"
                  }
                }
              }
            }
          }
        }
      }
    ]
  }
}
